import 'package:flutter/material.dart';
import 'package:smart_list/screens/addAnimal/components/body.dart';

class AddAnimalScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}